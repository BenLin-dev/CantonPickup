# dependencies
node_modules/
.pnp/
.pnp.js

# build output
dist/
*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
build.log

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# env (only if you add one later)
.env
.env.local
.env.*.local

# verify artifacts (qa shots, screenshots)
评价图工具/qa/shots/
评价图工具/out/chrome-profile/

# image-pipeline work areas
# `_src/` holds the raw AI renders that scripts/make-service-images.py reads from.
# They are 2.5 MB apiece and the converted output is committed, so the sources
# stay local. `_tmp_*` is scratch and must never be committed.
_src/
_tmp_*/
